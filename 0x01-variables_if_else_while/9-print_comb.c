#include <stdio.h>
/**
* main - prints all possible combinations of single-digit numbers
*
* Return: Always 0 (succes)
*/
int main(void)
{
int n;
for (n = 48; i < 58; i++)
{
putchar(n);
if (n != 57)
{
putchar(',');
putchar(' ');
}
}
putchar('\n');
return (0);
}
